<!DOCTYPE html>
<html>
  <head>
    <title>HouseSellingFinal</title>
    <%= csrf_meta_tags %>
 </head>
  <div class="container">
    <div class="header">
      <% if logged_in? and (current_page?(root_url)) %>
        <h3>Welcome <%= current_user.name %> to the House Hunting System</h3>
        <%= link_to 'Log out', logout_path, method: :delete %>
<% else %>
     <%= link_to 'Sign Up', new_user_path %> |
      <%= link_to 'Log in', new_session_path %> |
      <% end %>
    </div>
  </div>
  <body>
    <%= yield %>
  </body>
</html>
